import 'package:flutter/material.dart';

class ActiveThemeScreen extends StatelessWidget {
  const ActiveThemeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Active theme Screen",
          style: TextStyle(fontSize: 22)),
    );
  }
}
